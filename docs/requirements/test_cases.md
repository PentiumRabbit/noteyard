# noteyard 测试用例

---

## REQ-040 本地图床（图片上传）

| 用例ID | 用例名称 | 关联需求 | 前置条件 | 测试步骤 | 预期结果 | 状态 |
|--------|---------|---------|---------|---------|---------|------|
| TC-IMG-001 | PNG 上传成功 | REQ-040 | 服务端运行在 8080 | POST /api/uploads 上传 PNG 文件 | 返回 200，JSON 含 url 字段，url 以 /uploads/ 开头 | 已通过 |
| TC-IMG-002 | 非法格式被拒绝 | REQ-040 | 服务端运行 | POST /api/uploads 上传 .txt 文件 | 返回 415 | 已通过 |
| TC-IMG-003 | 缺少 file 字段 | REQ-040 | 服务端运行 | POST /api/uploads 不带 file 字段 | 返回 400 | 已通过 |
| TC-IMG-004 | JPEG 上传成功 | REQ-040 | 服务端运行 | POST /api/uploads 上传 JPEG | 返回 200，含 url | 已通过 |
| TC-IMG-005 | URL 格式正确 | REQ-040 | 服务端运行 | 上传文件后检查 url 字段 | url 以 http://localhost:8080/uploads/ 开头 | 已通过 |

---

## REQ-041 斜杠菜单中文拼音模糊匹配

| 用例ID | 用例名称 | 关联需求 | 前置条件 | 测试步骤 | 预期结果 | 状态 |
|--------|---------|---------|---------|---------|---------|------|
| TC-PIN-001 | 空查询返回 true | REQ-041 | pinyin-pro 已安装 | pinyinMatch("表格", "") | 返回 true | 已通过 |
| TC-PIN-002 | 直接子串匹配 | REQ-041 | — | pinyinMatch("表格", "表格") | 返回 true | 已通过 |
| TC-PIN-003 | 全拼匹配 | REQ-041 | — | pinyinMatch("表格", "biaoge") | 返回 true | 已通过 |
| TC-PIN-004 | 缩写匹配 | REQ-041 | — | pinyinMatch("中文拼音", "zwpy") | 返回 true | 已通过 |
| TC-PIN-005 | 不匹配返回 false | REQ-041 | — | pinyinMatch("表格", "xyz") | 返回 false | 已通过 |
| TC-PIN-006 | 大小写不敏感 | REQ-041 | — | pinyinMatch("表格", "BiaoGe") | 返回 true | 已通过 |

---

## REQ-039 个性化设置系统（字体 + 主题）

| 用例ID | 用例名称 | 关联需求 | 前置条件 | 测试步骤 | 预期结果 | 状态 |
|--------|---------|---------|---------|---------|---------|------|
| TC-SET-001 | 切换本地字体即时生效 | REQ-039 | 应用运行 | 打开设置面板 → 选择「Serif」| `--font-body` CSS 变量立即更新为衬线字体栈，编辑器正文字体改变，无加载延迟 | 已编写待执行 |
| TC-SET-002 | 切换本地主题即时生效 | REQ-039 | 应用运行 | 打开设置面板 → 点击「暗色」| `<html>` 上 `data-theme="dark"` 属性设置，界面切换为深色，过渡动画流畅 | 已编写待执行 |
| TC-SET-003 | 网络字体按需加载 | REQ-039 | 联网环境 | 选择「思源黑体」| 显示加载中 spinner，CSS 加载完成后编辑器切换字体 | 已编写待执行 |
| TC-SET-004 | 网络主题按需加载 | REQ-039 | 联网环境 | 选择「极光」主题 | 显示加载中，CSS 加载完成后界面切换为极光风格 | 已编写待执行 |
| TC-SET-005 | 字体偏好持久化 | REQ-039 | 应用运行 | 切换为「Serif」后刷新页面 | 刷新后自动恢复 Serif，localStorage noteyard_font 为 "serif" | 已编写待执行 |
| TC-SET-006 | 主题偏好持久化 | REQ-039 | 应用运行 | 切换为「暖米色」后刷新 | 刷新后自动应用暖米色，localStorage noteyard_theme 为 "warm" | 已编写待执行 |
| TC-SET-007 | 首次使用默认状态 | REQ-039 | 清空 localStorage | 打开应用 | 字体为 Default Sans，主题为默认亮色 | 已编写待执行 |
| TC-SET-008 | 代码块字体隔离 | REQ-039 | 应用运行 | 切换为「Serif」后查看含代码块的页面 | 代码块字体保持等宽，不跟随全局字体 | 已编写待执行 |
| TC-SET-009 | 网络字体缓存复用 | REQ-039 | 已加载过思源黑体 | 再次选择「思源黑体」 | 立即切换，无加载 spinner（DOM 中 style 标签已存在）| 已编写待执行 |
| TC-SET-010 | 网络主题标注显示 | REQ-039 | 应用运行 | 打开设置面板，查看主题区 | 极光和森林选项显示 🌐 标识，本地主题无标识 | 已编写待执行 |
| TC-SET-011 | 断网字体降级 | REQ-039 | 断网，无本地缓存 | 选择「思源黑体」 | 降级为 Default Sans，状态栏显示「无法加载网络字体，已使用默认字体」 | 已编写待执行 |
| TC-SET-012 | 断网主题降级 | REQ-039 | 断网，无本地缓存 | 选择「森林」主题 | 降级为默认亮色，状态栏显示「主题加载失败，已切换至默认亮色」 | 已编写待执行 |
