[checklist]
role       = "eng-frontend"
req        = "ISS-048"
dispatch   = 233
created_at = "2026-05-06"

[items]
review_read        = { done = true, note = "读取 ISS-048-review.md，理解根因与修复方案" }
impl_side_regular  = { done = true, note = "side=regular 分支改用 removeBlocks+insertBlocks，返回 true" }
tsc_clean          = { done = true, note = "npx tsc --noEmit 无报错" }
manual_test        = { done = false, note = "待手动验证" }

[summary]
status       = "delivered"
delivered_at = "2026-05-06"
note         = "dropOverlayPlugin.ts side=regular 分支已接管普通拖拽，使用 BlockNote API 移动 block，返回 true，跳过 ProseMirror 默认 replaceRange 路径，修复前导空白问题。side=left/right 分支不变。"
