[[items]]
id = "getPopoverY"
label = "在 DatabaseView.tsx 中封装 getPopoverY 纯函数"
done = true

[[items]]
id = "col-menu"
label = "col-menu openColMenu 使用 getPopoverY（估算高度 200px）"
done = true

[[items]]
id = "select-dropdown"
label = "select-dropdown openSelectDropdown 使用 getPopoverY（估算高度 180px）"
done = true

[[items]]
id = "multi-select-dropdown"
label = "multi-select openMultiSelectDropdown 使用 getPopoverY（估算高度 180px）"
done = true

[[items]]
id = "select-opts-popover"
label = "select-opts-popover openSelectOptions 使用 getPopoverY（估算高度 280px）"
done = true

[[items]]
id = "add-col-popover"
label = "add-col-popover openAddCol 使用 getPopoverY（估算高度 420px）"
done = true

[[items]]
id = "rollup-popover-databaseview"
label = "rollup-popover openRollupPopover 使用 getPopoverY（估算高度 320px）"
done = true

[[items]]
id = "rollup-popover-component"
label = "RollupConfigPopover.tsx 内部 adaptiveY 自适应检测 window.innerHeight"
done = true

[[items]]
id = "relation-picker"
label = "RelationCell.tsx picker 开启时检测视口空间，不足 240px 加 relation-picker--upward class"
done = true

[[items]]
id = "css"
label = "DatabaseView.css 补充 .relation-picker--upward { bottom: 100%; top: auto; }"
done = true
