# noteyard 发布前回归清单

每次发布前，逐项手动验证。全部通过后方可发布。

---

## 核心流程

### 页面管理

| # | 验证项 | 预期结果 |
|---|--------|---------|
| C-01 | 点击「+ 新建页面」创建顶级页面 | 侧边栏出现新页面，自动进入编辑 |
| C-02 | 悬停页面条目 → 点击「+」创建子页面 | 子页面出现在父页面下方 |
| C-03 | 侧边栏拖拽调整页面顺序 | 顺序变更后刷新保持不变 |
| C-04 | 侧边栏拖拽改变层级（变子页面 / 提升到根） | 层级变更正确，不丢失内容 |
| C-05 | 右键 → 重命名页面 | 名称更新，侧边栏和标题同步 |
| C-06 | 右键 → 删除页面 → 出现在回收站 | 侧边栏不再显示，回收站可见 |
| C-07 | 回收站还原页面 | 页面回到原位置，内容完整 |
| C-08 | 回收站永久删除 | 页面彻底消失，刷新后不再出现 |

### 编辑器基础

| # | 验证项 | 预期结果 |
|---|--------|---------|
| E-01 | 打开已有内容页面 | **内容正确加载，无空白**（ISS-010 / 本轮重点） |
| E-02 | 输入文字后 1s 内切换到另一页面，再切回 | 内容已保存，不丢失（ISS-001） |
| E-03 | 输入 `/` 打开斜杠菜单 | 菜单正常弹出，可插入各类块 |
| E-04 | 插入 H1 / H2 / H3 标题块 | 渲染正确，级别正确 |
| E-05 | 插入有序 / 无序列表，Tab 缩进 / Shift-Tab 取消缩进 | 缩进层级正确 |
| E-06 | 插入代码块，输入代码 | 代码块正常渲染 |
| E-07 | 插入分割线（Divider） | hr 正常显示 |
| E-08 | 插入引用块（Quote） | 左边框样式正确 |
| E-09 | 插入标注块（Callout），修改图标 | 图标选择器正常，内容可编辑 |
| E-10 | 插入折叠块（Toggle），展开/收起 | 状态切换正常 |
| E-11 | 选中文字 → 加粗 / 斜体 / 下划线 / 删除线 / 代码 | 格式工具栏出现，格式正确应用 |
| E-12 | 拖拽块手柄调整块顺序 | 顺序变更后保存正确 |
| E-13 | ⌘Z 撤销 / ⌘⇧Z 重做 | 历史正确回退/前进 |
| E-14 | ⌘K 全局搜索 | 搜索框弹出，结果可跳转 |

### 多列布局

| # | 验证项 | 预期结果 |
|---|--------|---------|
| M-01 | 插入分栏块（Two Columns） | 两列正常渲染，无崩溃 |
| M-02 | 列内插入标题、列表、代码块等子块 | 子块正常渲染，不崩溃（ISS-003/004） |
| M-03 | 打开已有含多列的页面 | 内容正确加载，无 RangeError（ISS-004） |
| M-04 | 列内容编辑后切换页面再切回 | 列内容保存正确 |

### 数据库块

| # | 验证项 | 预期结果 |
|---|--------|---------|
| D-01 | 在页面中插入数据库块 | 表格正常渲染，无空白 databaseId（ISS-003） |
| D-02 | 切换到看板 / 列表 / 日历视图 | 视图宽度正常，不被容器截断（ISS-006/008） |
| D-03 | 新增列 → 选择列类型 | 列类型下拉显示图标和名称（ISS-007） |
| D-04 | 靠近底部触发列类型下拉 | 菜单向上弹出，不被裁切（ISS-005） |
| D-05 | 添加 Select 列 → 单元格输入选项 | 选项正常保存和显示 |
| D-06 | 添加 Formula 列 → 输入公式引用其他列 | 公式计算正确，循环引用被拦截（ISS-002） |
| D-07 | 新增行 → 编辑单元格 → 切换页面再切回 | 数据不丢失 |
| D-08 | 删除列 / 删除行 | 操作生效，刷新后不恢复 |

### 媒体块

| # | 验证项 | 预期结果 |
|---|--------|---------|
| P-01 | 插入图片（上传或拖拽） | 图片正常显示，重启后仍可见 |
| P-02 | 插入文件附件，下载文件 | 下载链接有效 |
| P-03 | 插入书签，粘贴 URL | 封面/标题/描述正常抓取展示 |
| P-04 | 插入 PDF，预览 | PDF iframe 正常渲染 |

---

## 首次启动 / 新用户体验

| # | 验证项 | 预期结果 |
|---|--------|---------|
| F-01 | 全新安装（或清空 DB）后启动 | 侧边栏显示「欢迎使用 noteyard」页面（ISS-010） |
| F-02 | 打开欢迎页 | **页面有完整内容**（4 章节、标题、callout、列表等），不是空白（ISS-010 本轮重点） |
| F-03 | 启动时无防火墙弹窗 | macOS 不弹「是否接受传入连接」（已修复，监测回归） |

---

## 应用图标

| # | 验证项 | 预期结果 |
|---|--------|---------|
| I-01 | Dock 中图标显示 | 白色圆角方块 + 橙色斜体 N 字，有淡黄斜条（ISS-009） |
| I-02 | 关于对话框 / 应用信息图标 | 与 Dock 一致 |

---

## 稳定性

| # | 验证项 | 预期结果 |
|---|--------|---------|
| S-01 | 连续快速切换 10 个页面 | 无崩溃，最终停留页面内容正确 |
| S-02 | 编辑内容后直接关闭窗口再重开 | 内容已保存（beforeunload beacon） |
| S-03 | 后台运行 30 分钟后回到应用 | 心跳保存生效，无数据丢失 |
| S-04 | 重启应用 | 所有页面、数据库、侧边栏顺序完整恢复 |

---

## 已知待修问题（不阻断发布，但需记录状态）

| ISS | 描述 | 当前状态 |
|-----|------|---------|
| ISS-005 | 下拉菜单靠近底部被裁切 | 待修复 |
| ISS-006 | 数据库非表格视图宽度不足 | 待修复 |
| ISS-007 | 列类型选择器无图标 | 待修复 |
| ISS-008 | 数据库块宽度与正文不对齐 | 待修复 |

---

_更新：每次修复 Issue 后更新对应行状态；新 Issue 同步加入「待修问题」表格。_
